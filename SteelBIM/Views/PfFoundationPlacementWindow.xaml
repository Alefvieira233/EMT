<Window x:Class="SteelBIM.Views.PfFoundationPlacementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ECC - Lançar Fundação"
        Width="620"
        MinHeight="420"
        Background="{DynamicResource WindowBackgroundBrush}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SteelBIM;component/Views/Themes/AppTheme.Base.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0 0 0 12">
            <TextBlock Text="Lançamento de Fundação" Style="{StaticResource DialogTitleText}"/>
            <TextBlock Text="Localiza a base dos pilares e insere a fundação estrutural selecionada no centro de cada pilar, pela seleção atual ou por todos os pilares visíveis na vista."
                       Style="{StaticResource DialogSubtitleText}"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <GroupBox Header="Fundação" Margin="0 0 0 12">
                <Grid Margin="8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Família:" VerticalAlignment="Center" Margin="0 0 8 8"/>
                    <ComboBox x:Name="cmbFamilia" Grid.Row="0" Grid.Column="1" Margin="0 0 0 8" DisplayMemberPath="FamilyName" SelectionChanged="CmbFamilia_SelectionChanged"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Tipo:" VerticalAlignment="Center" Margin="0 0 8 0"/>
                    <ComboBox x:Name="cmbTipo" Grid.Row="1" Grid.Column="1" DisplayMemberPath="TypeName"/>
                </Grid>
            </GroupBox>

            <GroupBox Header="Escopo" Margin="0 0 0 12">
                <StackPanel Margin="8">
                    <RadioButton x:Name="rbSelecao" Content="Pilares da seleção atual" Margin="0 0 0 6"/>
                    <RadioButton x:Name="rbVista" Content="Todos os pilares visíveis na vista ativa"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Opções">
                <StackPanel Margin="8">
                    <CheckBox x:Name="chkOrientar" Content="Orientar a fundação pelo eixo local do pilar" IsChecked="True" Margin="0 0 0 8"/>
                    <CheckBox x:Name="chkIgnorarExistente" Content="Ignorar pilares que já tenham fundação próxima" IsChecked="True" Margin="0 0 0 8"/>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Tolerância de centro (mm):" VerticalAlignment="Center" Margin="0 0 8 0"/>
                        <TextBox x:Name="txtTolerancia" Width="90" Text="150"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Content="Cancelar" Width="90" Style="{StaticResource SecondaryActionButton}" Margin="0 0 8 0" Click="BtnCancel_Click"/>
            <Button x:Name="btnOk" Content="OK" Width="80" Style="{StaticResource PrimaryActionButton}" Click="BtnOk_Click"/>
        </StackPanel>
    </Grid>
</Window>
