<Window x:Class="FerramentaEMT.Views.ContraventamentoPlanoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurar Contraventamento"
        Width="620"
        Height="300"
        Background="{DynamicResource WindowBackgroundBrush}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FerramentaEMT;component/Views/Themes/AppTheme.Base.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Perfil do Contraventamento" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="18"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="140"/>
                </Grid.ColumnDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Família:" VerticalAlignment="Center"/>
                <ComboBox x:Name="cmbFamilia" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="4" Margin="6 2 0 2" SelectionChanged="CmbFamilia_SelectionChanged"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Perfil:" VerticalAlignment="Center"/>
                <ComboBox x:Name="cmbPerfil" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="4" Margin="6 2 0 2"/>

                <Label Grid.Row="2" Grid.Column="0" Content="Justificação Z:" VerticalAlignment="Center"/>
                <ComboBox x:Name="cmbZJust" Grid.Row="2" Grid.Column="1" Width="150" Margin="6 2 0 2" HorizontalAlignment="Left"/>

                <Label Grid.Row="2" Grid.Column="3" Content="Offset 2ª diagonal (mm):" VerticalAlignment="Center" Margin="0 0 6 0"/>
                <TextBox x:Name="numOffsetSegundaDiagonal" Grid.Row="2" Grid.Column="4" Width="100" Margin="0 2 0 2" HorizontalAlignment="Left" Text="30"/>

                <CheckBox x:Name="chkDesabilitarUniao" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="5" Margin="0 10 0 0" Content="Desabilitar união nas barras" IsChecked="True"/>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 12 0 0">
            <Button x:Name="btnCancel" Content="Cancelar" Width="90" Margin="0 0 8 0" Style="{StaticResource SecondaryActionButton}"/>
            <Button x:Name="btnOk" Content="OK" Width="80" Style="{StaticResource PrimaryActionButton}"/>
        </StackPanel>
    </Grid>
</Window>
