<Window x:Class="FerramentaEMT.Views.PfNamingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Numerar Automatico"
        Width="760"
        Height="560"
        MinWidth="700"
        MinHeight="500"
        MaxHeight="720"
        Background="{DynamicResource WindowBackgroundBrush}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FerramentaEMT;component/Views/Themes/AppTheme.Base.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0 0 0 12">
            <TextBlock Text="Numerar Automatico" Style="{StaticResource DialogTitleText}"/>
            <TextBlock Text="Escolha o alvo PM, filtre por familia e tipo e defina o parametro que recebera a sequencia automatica."
                       Style="{StaticResource DialogSubtitleText}"/>
        </StackPanel>

        <GroupBox Grid.Row="1" Header="Escopo da busca">
            <StackPanel Orientation="Horizontal">
                <RadioButton x:Name="rbModeloInteiro" Content="Modelo inteiro" Checked="Scope_Checked"/>
                <RadioButton x:Name="rbVistaAtiva" Content="Vista ativa" Checked="Scope_Checked"/>
                <RadioButton x:Name="rbSelecaoAtual" Content="Selecao atual" Checked="Scope_Checked"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="2" Header="Conjunto PM">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Alvo:" VerticalAlignment="Center"/>
                <ComboBox x:Name="cmbAlvo" Grid.Row="0" Grid.Column="1" MinWidth="220" Margin="4,0" SelectionChanged="Alvo_SelectionChanged"/>

                <Label Grid.Row="0" Grid.Column="2" Content="Familia:" VerticalAlignment="Center" Margin="20,0,0,0"/>
                <ComboBox x:Name="cmbFamilia" Grid.Row="0" Grid.Column="3" MinWidth="220" Margin="4,0" SelectionChanged="Familia_SelectionChanged"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Tipo:" VerticalAlignment="Center"/>
                <ComboBox x:Name="cmbTipo" Grid.Row="1" Grid.Column="1" MinWidth="220" Margin="4,0" SelectionChanged="Tipo_SelectionChanged"/>

                <Label Grid.Row="1" Grid.Column="2" Content="Parametro:" VerticalAlignment="Center" Margin="20,0,0,0"/>
                <ComboBox x:Name="cmbParametro" Grid.Row="1" Grid.Column="3" MinWidth="220" Margin="4,0" SelectionChanged="Parametro_SelectionChanged"/>

                <Border Grid.Row="2" Grid.ColumnSpan="4" Margin="0,10,0,0" Style="{StaticResource WarningCardBorder}">
                    <TextBlock TextWrapping="Wrap" Foreground="{DynamicResource WarningTextBrush}">
                        A sequencia e aplicada em um unico passo. Em vigas, a ordem prioriza horizontais/X e depois verticais/Y antes da leitura visual da vista.
                    </TextBlock>
                </Border>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Sequencia">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Label Grid.Row="0" Grid.Column="0" Content="Prefixo:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtPrefixo" Grid.Row="0" Grid.Column="1" Margin="4,0" TextChanged="Valor_TextChanged"/>

                <Label Grid.Row="0" Grid.Column="2" Content="Sufixo:" VerticalAlignment="Center" Margin="20,0,0,0"/>
                <TextBox x:Name="txtSufixo" Grid.Row="0" Grid.Column="3" Margin="4,0" TextChanged="Valor_TextChanged"/>

                <Label Grid.Row="1" Grid.Column="0" Content="Inicio:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtInicio" Grid.Row="1" Grid.Column="1" Width="120" Margin="4,0" TextChanged="Valor_TextChanged"/>

                <Label Grid.Row="1" Grid.Column="2" Content="Degrau:" VerticalAlignment="Center" Margin="20,0,0,0"/>
                <TextBox x:Name="txtDegrau" Grid.Row="1" Grid.Column="3" Width="120" Margin="4,0" TextChanged="Valor_TextChanged"/>

                <Border Grid.Row="2" Grid.ColumnSpan="4" Margin="0,10,0,0" Style="{StaticResource AccentCardBorder}">
                    <TextBlock x:Name="txtPreview"
                               FontWeight="Bold"
                               Foreground="{DynamicResource AccentCardTextBrush}"
                               Text="Proximo valor: 1"/>
                </Border>
            </Grid>
        </GroupBox>

        <GroupBox Grid.Row="4" Header="Resumo e itens">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Style="{StaticResource SubtleCardBorder}">
                    <TextBlock x:Name="txtResumo" TextWrapping="Wrap"/>
                </Border>

                <GroupBox Grid.Row="1" Header="Itens filtrados" Margin="0,12,0,0">
                    <ListBox x:Name="lstElementos"
                             MinHeight="150"
                             BorderThickness="0"
                             Background="Transparent"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"/>
                </GroupBox>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Content="Cancelar" Width="96" Style="{StaticResource SecondaryActionButton}" Margin="0,0,8,0"/>
            <Button x:Name="btnOk" Content="Aplicar" Width="110" Style="{StaticResource PrimaryActionButton}"/>
        </StackPanel>
    </Grid>
</Window>
